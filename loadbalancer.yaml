apiVersion: v1
kind: Service
metadata:
  name: nginx-lb
  namespace: deployment-ns
spec:
  type: LoadBalancer

  selector:
    app: nginx-pod

  ports:
    - port: 80 # external port
      targetPort: 80 # container port
